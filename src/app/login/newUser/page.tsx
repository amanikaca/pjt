export default function Home() {
    return (
      <div>
        <p>
          in this you need to  ask question from the new suer ot eh app like 
          name,depatement,year 
          according to this after this in home page the time table,will show up 
          the user will need to manually update this in profile page
        </p>
      </div>
    );
  }